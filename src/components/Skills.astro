<!-- components/Skills.astro -->
---
const {skills}=Astro.props;
---
<section>
  <h2>Habilidades</h2>
  <ul>{skills.map(skill => {
    return (
    <li>{skill}</li>)}
    )}   
  </ul>
  
</section>

